version: '3.8'

services:
  # 1. Vector Database
  qdrant:
    image: qdrant/qdrant:latest
    container_name: speckit-qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - speckit-network

  # 2. Graph Database
  neo4j:
    image: neo4j:5.17.0
    container_name: speckit-neo4j
    ports:
      - "7474:7474" # HTTP
      - "7687:7687" # Bolt
    environment:
      - NEO4J_AUTH=neo4j/password # Change in production
      - NEO4J_PLUGINS=["apoc"]
    volumes:
      - neo4j_data:/data
    networks:
      - speckit-network

  # 3. Backend (FastAPI)
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    container_name: speckit-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env.docker
    # environment:
    #   - MAX_WORKERS=1 # Reload logic handles workers in dev
    depends_on:
      - neo4j
      - qdrant
    networks:
      - speckit-network
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  # 4. Frontend (Nuxt 3)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: speckit-frontend
    ports:
      - "3000:3000"
    environment:
      - NUXT_PUBLIC_API_BASE=http://localhost:8000/api/v1 # Access from browser
      - PORT=3000
    depends_on:
      - backend
    networks:
      - speckit-network

volumes:
  neo4j_data:
  qdrant_data:

networks:
  speckit-network:
    driver: bridge
